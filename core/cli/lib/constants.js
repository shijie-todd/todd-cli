'use strict';

const LOWEST_NODE_VERSION = '12.0.0';

// log levels
const DEBUG_LEVEL = 'verbose';
const DEFAULT_LEVEL = 'info';

module.exports = {
  LOWEST_NODE_VERSION,
  DEBUG_LEVEL,
  DEFAULT_LEVEL
}
