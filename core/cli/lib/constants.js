'use strict';

const LOWEST_NODE_VERSION = '12.0.0';
const CLI_HOME_PATH = '.todd-cli'

// log levels
const DEBUG_LEVEL = 'verbose';
const DEFAULT_LEVEL = 'info';

module.exports = {
  LOWEST_NODE_VERSION,
  CLI_HOME_PATH,
  DEBUG_LEVEL,
  DEFAULT_LEVEL
}
